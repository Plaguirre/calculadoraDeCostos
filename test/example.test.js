/**
 * Placeholder — instalar dependencias para correr los tests reales:
 * npm install --save-dev jest supertest mongodb-memory-server
 * 
 * Ver auth.test.js y materials.test.js para los tests de integración.
 */
describe('Placeholder', () => {
  it('pasa', () => expect(true).toBe(true));
});
